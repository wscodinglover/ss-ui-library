import React from 'react';
// import {ConfigConsumer} from './context'

// eslint-disable-next-line react/prefer-stateless-function
class ConfigProvider extends React.Component<any, any> {
  render() {
    return (
      <div>
        wqeqwe
      </div>
    );
  }
}

export default ConfigProvider;
